# 资金规划置顶提示

按用户要求，预算概览前展示默认展开的提示卡，沿用现有字体、颜色和自适应宽度。无提示时不保留空白区域。

提示内容来自鉴权后的预算接口：以 `置顶提示：` 开头的 rules 条目。公开代码只处理该通用标记，不含用户的金额或具体私有提示。渲染时转义文本，置顶条目不在折叠说明中重复。

无需扩展云端数据结构，旧快照仍然兼容。退出登录时沿用现有逻辑清空资金内容。验证覆盖默认展开、显示顺序、文本转义、取消提示后的清除及旧快照兼容。
